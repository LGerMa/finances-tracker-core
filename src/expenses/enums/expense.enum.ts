export enum PaymentMethod {
  CASH = 'cash',
  DEBIT_CARD = 'debit_card',
  CREDIT_CARD = 'credit_card',
  TRANSFER = 'transfer',
}

export enum ExpenseType {
  FIXED = 'fixed',
  VARIABLE = 'variable',
  UNPLANNED = 'unplanned',
  PLANNED = 'planned',
  SAVING = 'saving',
}
